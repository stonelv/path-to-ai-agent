# 能力路线

[返回首页](../README.md) · [验收指南](./assessment.md) · [岗位分轨](./career-tracks.md) · [课程依据](./curriculum-sources.md)

按能力依赖学习，时间只作参考。已有经验者可提交相应验收证据后跳过补课，不必重复按天打卡。
默认主线是应用工程；平台与推理基础设施在共同基础后按目标岗位选修。模块编号和现有项目路径保持不变。

## 主线与前置依赖

| 模块 | 前置 | 必须能回答的问题 | 产出 |
| --- | --- | --- | --- |
| 00 环境与工程补课 | Git、HTTP、基本编程 | 如何隔离环境并验证安装？ | 独立环境、测试、配置说明 |
| 01 可靠 LLM 调用 | 00 | 怎样隔离供应商、识别错误并限制重试？ | 文本调用、错误测试、实验记录 |
| 02 结构化输出与评估 | 01 | 如何把不可信文本变成可验证数据？ | Schema、标注集、基线、提取 API |
| 03 工具调用、单 Agent 与基础 MCP | 02 | 如何选择并约束工具，而不是无限循环？ | 只读工具、最小 Loop、MCP client 接入、轨迹与终态评估 |
| 04 上下文工程、RAG 与记忆 | 03；独立检索实验可从 02 开始 | 如何选择、保留、隔离并删除任务所需的信息？ | 上下文预算、检索与引用、记忆生命周期和质量评估 |
| 05 状态与业务流程 | 03、04 | 中断恢复后如何避免重复写操作？ | 工单状态机、审批、持久化、幂等测试 |
| 06 MCP 服务、Skills 与系统集成 | 03、04；主线建议在 05 后 | 如何提供可复用且受控的工具与任务流程？ | MCP Server、认证授权、审计、有测试的 Skill |
| 07 生产化与综合项目 | 05、06 | 如何衡量业务收益、发布、接管和回滚系统？ | 产品交付、评估 CI、观测、成本与故障演练 |

当前可学习范围见[首页状态表](../README.md#课程状态)。这里的产出是目标，不是当前功能清单。

## 关键模块的必修任务

### 模块 03：先做受控工具，再连接 MCP

- 用代码定义工具参数、输出、错误和权限契约；工具结果属于不可信数据。
- 实现有最大步骤数、超时、预算及终止原因的最小 Loop，与确定性工作流比较。
- 理解 MCP 的 host/client/server，使用 client 接入一个只读测试服务；不要求此时实现生产 Server。
- 从首个 Agent 开始保存小型任务集，验证工具参数、调用边界和最终结果。
- 加入恶意工具输出、拒绝授权和失败中止案例，不靠提示词代替权限校验。

### 模块 04：上下文、检索和记忆分别验收

1. **上下文工程**：固定任务，比较全量历史与按需加载；控制工具输出、Token 预算和压缩，检查关键约束是否丢失。
2. **RAG**：分开评估检索与回答，覆盖引用、拒答、权限过滤、更新和删除；需要时再引入混合检索与重排序。
3. **记忆**：区分本轮上下文、会话状态、业务执行状态和跨会话记忆；定义写入、来源、更新、失效、删除和租户隔离。

必做实验：同一任务在大量工具输出下比较成功率、延迟和成本；过期记忆应能纠正，用户 A 的记忆不得影响用户 B。
长期记忆不等于把聊天记录全部存进向量数据库，checkpoint 也不等于保证外部副作用只执行一次。

### 模块 06：分清 Tool、MCP 和 Skill

- Tool 执行具体操作；MCP 提供工具与上下文的互操作协议；Skill 组织任务知识、指令、脚本与资源。
- 为 MCP Server 增加身份、资源授权、输出限制和审计；凭据不直接交给模型生成的代码。
- 封装一个按需加载的 Skill，验证适用范围、资源加载和失败处理；安装第三方 Skill 前审查脚本与数据访问。
- 学习远程授权边界与协议安全实践，不使用 token passthrough，不把用户确认当作唯一安全机制。

### 模块 07：交付证据而不是功能清单

从早期模块逐步积累评估、Trace 和最小部署，在此统一验证灰度、回滚、告警和人工接管。
至少完成一次模型或供应商切换的同集回归；分别核对结构化输出、工具、流式事件与用量字段，
不假设兼容 API 就意味着能力一致。不自动发送私人数据或产生未经确认的费用。

## 项目演进

### A. 航司行李额提取器（00～02）

保留[当前项目](../month-01-llm-foundations)，逐步完成：

```text
文本调用 → 结构化输出 → 领域校验 → 固定评估 → API → 基础部署
```

不涉及真实订座、收费或自动抓取；原文没提供的信息不靠常识补全。
这是可靠 LLM 应用基础，无需为它强加 Agent Loop。以[第一月必做范围](../month-01-llm-foundations/README.md#25-第一版范围)为准，
不以全面覆盖航司政策或凑满 100 条数据作为进入 Agent 学习的唯一门槛。

### B. 研发知识与工具助手（03、04、06）

先接入只读工具和基础 MCP，再为知识检索、上下文筛选与记忆分别建立基线，最后将工具以受控 MCP 服务提供并封装 Skill。
知识数据默认使用可分发的合成资料；测试中明确区分用户权限，验证记忆更新、删除和隔离。
代码审查助手可作为 Prompt / Precision-Recall 实验，不额外维护一个完整应用。

### C. 可恢复的工单 Agent（05、07）

在模拟业务系统中完成分类、补全、审批与状态更新。
用进程重启、重复请求、审批拒绝等案例证明恢复与幂等，而不只是展示成功路径。
真实外部写操作属于显式启用的进阶任务，默认不连接生产系统。

## 每个模块都要做的事

- **评估**：从少量有期望结果的案例开始，随能力增加而扩充；区分程序测试和模型质量。
- **安全**：配置与数据脱敏、输入验证、工具最小权限；接入工具时加入提示词注入测试，有副作用前引入审批和审计。
- **观测**：先记录模型、请求 ID、延迟与失败；后续扩充 Token、成本、Trace 和告警。
- **可靠性**：明确超时、失败和终止；有写操作后再验证幂等、补偿和恢复。
- **AI 编程协作**：每个项目完成一次[小任务委派与验收](./coding-agent-workflow.md)，提交可解释的 diff、测试和失败复盘。
- **业务交付**：记录原流程、模型必要性、用户反馈或模拟评价、收益指标，以及不允许自动执行的边界。

不把这些内容全部推迟到年度计划的第 9～11 月。
普通程序回归与真实模型评估采用不同的 CI 触发和预算边界，具体要求见[Agent 验收](./assessment.md#agent-专项验收规划)。

## 选修与节奏

- 先手写简单且有边界的 Agent Loop，再用一个主框架重构。后续教学参考优先选择 LangGraph 的状态与持久化能力，
  但这不是唯一就业技术栈；尚未添加框架依赖，具体版本在实现时验证。
- 多 Agent 为选修：在相同任务集上与普通工作流、单 Agent 比较成功率、成本和延迟。
- 应用轨可增加一个图像、文档或语音输入扩展；Agent 平台轨深入运行时与治理；推理基础设施轨深入 vLLM、容量和云原生。
- A2A、微调、蒸馏和复杂自主规划按岗位或失败证据选修，不作为所有转型者的前置要求。
- 可以每周投入 10～15 小时，参考 20% 阅读、60% 实践、20% 测试复盘；以实际掌握为准。
- [原始年度计划](./annual-plan.md)保留原选题和时间安排，不要求每个月都新建项目。

每个毕业项目使用[交付报告模板](../templates/project-report.md)，包含业务问题、非模型基线、架构与取舍、运行方式、
测试与评估、反馈、失败接管和已知限制。不把框架数量、Agent 数量或演示效果当作能力证明。
公开演示前增加认证、限流、费用约束和数据审查，不把学习用服务裸露到公网。
