# FastAPI 服务化 AI 编程协作复盘

记录日期：2026-09-14

## 任务与边界

- 委派目标：将已有 `BaggageExtractor` 封装为本地 FastAPI 服务。
- 必须实现：`POST /v1/extractions`、`GET /health`、`GET /ready`、稳定错误契约、请求 ID、连接复用和单进程并发门禁。
- 允许修改：第一月项目源码、测试、依赖、课程与运行文档。
- 不在范围：认证、公网部署、分布式限流、数据库、RAG、Agent Loop 和真实业务写操作。
- 验收：常规测试不访问模型；完整 pytest 与 Ruff 通过；本地健康和就绪冒烟成功。

## 提供的必要上下文

- 现有 Extractor、Provider、配置、领域模型和错误类型。
- 第 6 课固定评估与真实调用的费用边界。
- 第一月项目的 API 里程碑和课程文档同步规则。
- 不泄露 `.env`、不记录政策正文、不把失败转换为空成功结果的约束。

## 实现与人工审查

- AI 先给出实现方案，经用户确认后开始实现。
- 采用应用工厂和 FastAPI lifespan，HTTP 层不复制 Prompt、JSON 解析或领域校验。
- 异常按请求、就绪、容量、认证、限流、超时、供应商和模型输出分类。
- 请求 ID 同时进入响应头、成功正文或错误正文；非法客户端 ID 会被替换。
- 使用信号量限制单进程模型并发，明确说明它不是生产级限流。
- 提交 `e6d9bc9` 包含 FastAPI 服务、第 7 课与离线测试。

审查中确认：

- `.env` 和真实评估输出继续被 Git 忽略。
- 健康与就绪检查不调用模型，不产生探针费用。
- API 错误不返回供应商原始详情、模型正文或密钥。
- 日志只记录方法、路径、状态、耗时和请求 ID。
- README、课程索引、环境指南和验收表与实现同步。

## 发现的问题与修正

1. Ruff 发现测试导入顺序不符合规范；按现有工具自动要求调整后复查通过。
2. 文档最初仍写“只有三个真实模型入口”和“API 规划中”；实现后同步改为四个入口和已交付状态。
3. 本机没有 Docker 命令，无法把 Dockerfile 存在写成“容器已验证”；交付报告明确标记未实机验证。
4. 真实 API 冒烟中模型漏掉合成航司名称；未在 API 层猜测或修补，而是记录为模型质量限制。

## 验证证据

- FastAPI 与配置针对性测试：34 项通过。
- 实现完成时完整回归：152 项通过。
- 2026-09-14 收尾复验：152 项通过，Ruff 通过，`git diff --check` 通过。
- 本地 Uvicorn：健康与就绪接口均为 200。
- 真实 API：合成文本提取为 200，请求 ID 贯穿响应，日志未包含原文。

## 未验证与禁止事项

- Docker 镜像构建与容器运行未验证，因为当前机器没有 Docker。
- 没有执行公网发布、真实旅客数据处理、生产权限测试或业务写操作。
- Token、费用、实际重试次数和生产运行指标尚未采集。
- 本复盘是脱敏摘要，不保存完整会话、密钥、原始供应商错误或私有思维过程。
