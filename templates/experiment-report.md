# 模型实验报告

报告必须区分人工观察、自动评分和推测；不要只保留成功结果。

## 问题与假设

- 想验证的工程问题：
- 本次唯一改变的变量（如有多个，说明无法单独归因）：
- 固定的输入、参数与对照组：
- 预先定义的评分方式与通过条件：

## 可复现信息

- 时间 / 操作系统 / Python / 依赖版本：
- 代码版本：
- 供应商与模型标识（不包含密钥）：
- Prompt / Schema / 数据版本：
- 工具 / Skill / 工作流版本，及测试身份与权限（适用时）：
- 数据来源、授权条件或合成标记：
- 开发集 / 留出集与样本数：
- 重复次数、预计请求量与预算控制：
- 执行命令：

## 结果

| 设置 / 样例 ID | 尝试次数 | 成功或错误 | 质量结果 | 延迟（是否含重试） | Token / 费用 |
| --- | --- | --- | --- | --- | --- |
| 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 未采集时明确标注 |

保留失败计数与波动，写清每个指标的分母。不要把超时或无效响应从结果中静默删除。
粘贴输出前检查隐私与再分发权限。

## Agent 实验补充（适用时）

- 任务初始环境、预期业务终态和允许操作：
- 每次 trial 前的环境重置/隔离方式：
- 必要轨迹约束、终态核对方法与脱敏 Trace 引用：
- 成功、失败、取消、预算终止和人工接管的计数与分母：
- 步数 / 超时 / 成本预算，实际终止原因：
- 中断恢复、重复副作用、授权/注入测试结果：
- 上下文预算、记忆更新/删除与租户隔离结果：
- 评分方法：代码断言 / 人工 / 模型；模型评分的抽查依据：

不要以唯一工具调用序列约束所有正确实现，不把模型说“完成”当作业务终态，也不要求保存私有思维链。
普通提取实验不适用的项目标记“不适用”，未执行的检查标记“未验证”。

## 分析与限制

- 证据支持什么结论：
- 证据不能支持什么结论：
- 最重要的失败类型：
- 下一次只改进哪个变量：
